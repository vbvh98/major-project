import React from 'react'

const Modal = ({ data, display }) => {
  return (
    <div style={{ display: display ? 'flex' : 'none' }} className='modal'>
      MOdal
    </div>
  )
}
export default Modal
